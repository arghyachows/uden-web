import React from 'react'
import { hot } from 'react-hot-loader'
import './App.scss'

const App = () => (
    <div className="app">
        <h1>UDEN!!!</h1>
    </div>
);

export default hot(module)(App);